@using QLBanHang_CongTyHoaHasfarmDaLat.Models;
@model List<NhaCungCap>
<div class="form-group">
    <label>Nhà Cung Cấp:</label>
    <select class="form-control" id="exampleFormControlSelect1" name="ncc" required>
        @for (int i = 0; i < Model.Count; i++)
        {
            <option value="@Model[i].MaNhaCungCap" @(i == 1 ? "selected" : "")>
                @Model[i].TenNhaCungCap
            </option>
        }
    </select>
</div>